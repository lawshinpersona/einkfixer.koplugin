local _ = require("gettext")

return {
    fullname = _("E-ink Fixer"),
    description = _([[Keeps a tiny clock region refreshing with no-merge UI refreshes, without requesting global/full refreshes.]]),
}
